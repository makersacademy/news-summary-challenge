// Feed get request on each page load
// Current feed data stored in newsFeed object
// When articles are loaded, can click on individual news item to get the body content - does each article therefore need an ID?