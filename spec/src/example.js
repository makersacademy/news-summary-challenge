(function (exports) {
  function testStoreNotes() {
    //var noteList = new NoteListModel();
    //noteList.storeNotes('bla bla');

    assert.isTrue(subject === 'expected');
  }
  testStoreNotes();
})(this);
