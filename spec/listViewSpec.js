'use strict';

describe('list view', function () {
  
  describe('renderList', function () {
    
    it('renders the HTML for a list of news articles', function () {
      expect()
    });
  
  });

});
