var list = new List();
var article;

var testList = [

  function() {
    list.addArticle(article)
    testEquals('Articles can be added to the list',
    list.articles.length,
    1
    )
  },


]

voodoo(testList)
