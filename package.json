{
  "devDependencies": {
    "jest": "^27.5.0"
  },
  "scripts": {
    "jest": "jest ./__tests__ --coverage",
    "build": "esbuild index.js --bundle  --outfile=bundle.js --watch"
  },
  "dependencies": {
    "esbuild": "^0.14.18",
    "jest-fetch-mock": "^3.0.3"
  }
}
