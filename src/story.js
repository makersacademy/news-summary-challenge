(function(exports) {

    function Story(headline, summary, image, fullStory) {
      this.headline = headline;
      this.summary = summary;
      this.image = image;
      this.fullStory = fullStory;
    }

    exports.Story = Story;
})(this);
