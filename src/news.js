class News {
    constructor(title, apiUrl, webUrl, imageLink, id) {
      this.title = title;
      this.apiUrl = apiUrl;
      this.webUrl = webUrl;
      this.imageLink = imageLink;
      this.id = id;
    }
}
