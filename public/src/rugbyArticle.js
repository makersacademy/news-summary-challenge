"use strict";

(function(exports) {
  function RugbyArticle(webTitle, webUrl, id) {
    this.title = webTitle;
    this.url = webUrl;
    this.id = id
  };

  exports.RugbyArticle = RugbyArticle;
})(this);
