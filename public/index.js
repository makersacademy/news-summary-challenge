window.addEventListener('load', (event) => {
  // let getBody = article.response.content.fields.body;
  // let app = document.getElementById('app');
  // let hiddenDiv = document.createElement('div');
  // hiddenDiv.style.visibility = 'hidden';
  // hiddenDiv.innerHTML = getBody;
  // let blockDiv = hiddenDiv.getElementsByClassName('block-elements');
  // let headers = [...blockDiv].map((each) => {
  //   let pTags = each.getElementsByTagName('p');
  //   return pTags[0].innerHTML;
  // });
  // let firstP = [...blockDiv][0].getElementsByTagName('p');
  // app.innerHTML = headers.join(' ');
  // console.log(headers);
});
