describe('pass', () => {
  it('looks like this', () => {
    expect(2 + 2).toEqual(4)
  })
})

describe('pass', () => {
  it('looks like this', () => {
    expect(2 + 2).toEqual(4)
  })
})

describe('pass', () => {
  it('looks like this', () => {
    expect(2 + 2).toEqual(4)
  })
})

describe('pass', () => {
  it('looks like this', () => {
    expect(2 + 2).toEqual(4)
  })
  it('looks like this', () => {
    expect(2 + 2).toEqual(4)
  })
})
