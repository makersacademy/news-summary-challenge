describe('Interface', () => {
  describe('showHeadlinesAndPictures', () => {
    it('renders the headlines and pictures', () => {
      // frontPage.articles = [{headline: 'Test', thumb}]
      // expect(headlines).toInclude('Test')
    })
  })
});
